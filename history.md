# Collaboration Record
| 2026-05-10 16:46 Asia/Shanghai | Codex | Refined the new full-panel Films Detail backdrop after screenshot feedback that the visible crop was ugly. Kept the no-rectangle dissolve approach, but adjusted the still crop anchor/mask and separated real backdrop stills from poster fallback rendering so portrait posters are contained as faint atmosphere instead of being cover-cropped into a huge body/head close-up. Cache busts: `app.js?v=237`, `films-components.js?v=41`, `media-library.css?v=223`. |
| 2026-05-10 16:34 Asia/Shanghai | Codex | Refined only the Films Detail panel/backdrop blending after user feedback that the right-side still still read as a rectangular inserted image. Replaced the right-side fixed backdrop container with a full-panel absolute atmosphere layer, revealed by a soft upper-right radial mask plus text-protection and lower-left dissolve overlays. Outer green/teal app shell, sidebar/topbar, routing, data, and JS behavior were unchanged. Cache bust: `media-library.css?v=222`. |
| 2026-05-10 16:13 Asia/Shanghai | Codex | Added automatic TMDb backdrop rotation for Film Detail without changing layout/sidebar/topbar. TMDb detail requests now include `images`, MovieCache stores normalized `backdropPaths`, and Film Detail rotates through available TMDb backdrops every 7.2s with a soft opacity transition. Local `backdropUrlOverride` still takes precedence and disables auto-rotation for that film; notes/details editing, hidden tabs, and reduced-motion mode pause the rotation. Cache busts: `app.js?v=236`, `components.js?v=91`, `films-components.js?v=40`, `media-library.css?v=221`. **Validation**: syntax checks for touched frontend/backend/test files; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js test/tmdbClient.test.js test/lightChromeCss.test.js` (116 passing). |
| 2026-05-10 15:42 Asia/Shanghai | Codex | Reverted the Films Detail backdrop image scale back to the earlier accepted `scale(1.02)` after the `0.8` experiment felt too small. Cache bust: `media-library.css?v=220`. **Validation**: `git diff --check` passed. |
| 2026-05-10 15:38 Asia/Shanghai | Codex | Further reduced the Films Detail backdrop crop/zoom per live feedback by changing the backdrop image transform from `scale(0.96)` to `scale(0.8)`. Cache bust: `media-library.css?v=219`. **Validation**: `git diff --check` passed. |
| 2026-05-10 15:33 Asia/Shanghai | Codex | Narrowly reduced the Films Detail right-side backdrop crop/zoom feel without changing layout, data, routing, or component structure. The backdrop image transform changed from `scale(1.02)` to `scale(0.96)` so TMDb stills feel less over-enlarged while keeping the existing atmospheric fade and mask behavior. Cache bust: `media-library.css?v=218`. **Validation**: `git diff --check` passed. |
| 2026-05-10 15:20 Asia/Shanghai | Codex | Continued Films module completion as a private collection, not a social feature. Reworked the existing `Mark as Rewatch` detail action into real private watch-history data: `UserMovieEntry.watchEvents` now stores local watch dates while `watchedAt` remains the primary/recent watched date, and the data stays out of TMDb `MovieCache`. First watched saves and rewatch saves append/maintain watch events through `/api/manage/movies`, and Film Detail now shows a restrained private `Watch history` section with count/date chips. Cache busts: `app.js?v=235`, `films-components.js?v=39`, `media-library.css?v=217`. **Validation**: `node --check` for movie repository, app, films components, movie/route/preview tests; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js test/tmdbClient.test.js test/lightChromeCss.test.js` (116 passing). |
| 2026-05-10 14:57 Asia/Shanghai | Codex | Audited and completed the Films backend/data-completeness pass without UI redesign. Confirmed `/api/manage/movies` exists and Films persistence intentionally uses the existing settings/KV/D1-adapter style (`manage@sysConfig@movieCache@${tmdbId}` plus `manage@sysConfig@userMovieEntries`) rather than a dedicated SQL `movies` table. Removed dead legacy Films modal/ticket/search component code and CSS remnants. Added local-only `UserMovieEntry` metadata/image overrides for title/original title/director/release/runtime/genres/synopsis/poster/backdrop, with patch-only saves preserving status/rating/favourite/watched date/journal and keeping MovieCache as TMDb/external data. Film Detail now has inline autosaving Edit Details for saved entries only, and poster/backdrop overrides preview immediately while editing. Cache busts: `app.js?v=234`, `films-components.js?v=38`, `media-library.css?v=216`. **Validation**: `node --check` for app, films components, movie repository, movies route, previewActions; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js test/tmdbClient.test.js test/lightChromeCss.test.js` (114 passing). |
| 2026-05-10 14:23 Asia/Shanghai | Codex | Tightened a remaining Films preview/save footgun. Unsaved TMDb detail previews are now true read-only previews for local-only controls: rating slider, status chips, watched-date input, notes/favourite/rewatch/remove controls are withheld until the movie is explicitly saved, and `saveFilmRating()` / `saveFilmWatchedDate()` also guard against unsaved transient detail records so stale DOM cannot accidentally create a local entry. The only save affordances in an unsaved preview are now explicit Add to Watchlist / Mark Watched, and using them from detail keeps the user in detail while converting the route to the saved entry with a hash replace instead of polluting browser back history. Cache busts: `app.js?v=233`, `films-components.js?v=37`, `media-library.css?v=215`. **Validation**: `node --check` for `app.js`, `films-components.js`, `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js test/tmdbClient.test.js test/lightChromeCss.test.js` (109 passing). |
| 2026-05-10 14:16 Asia/Shanghai | Codex | Continued Films interaction-chain polish without changing the app shell or route model. TMDb search-result detail opens now feel instant: clicking a result immediately opens an unsaved transient Film Detail from the search/local record while the full TMDb detail refreshes in the background, with an in-flight guard so repeated clicks do not push/open/fetch multiple times. Search results now show existing local Films state (`In Films · status`) and disable the already-current status button instead of looking unsaved. Replaced the Film Detail `...` placeholder with a real silent optimistic `Remove from Films` action for saved entries; unsaved TMDb detail previews show only explicit Add to Watchlist / Mark Watched actions. Cache busts: `app.js?v=232`, `films-components.js?v=36`, `media-library.css?v=214`. **Validation**: `node --check` for `app.js`, `films-components.js`, `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js test/tmdbClient.test.js test/lightChromeCss.test.js` (109 passing). |
| 2026-05-10 13:57 Asia/Shanghai | Codex | Fixed a Films TMDb search-detail regression where clicking a search result to inspect details could create a local saved Film entry. `openTmdbFilmDetail()` now opens a transient detail record only and does not upsert into `state.films`; explicit status/rating/date/note actions still convert that transient movie into a saved `UserMovieEntry`. Added a one-shot title-based cleanup for the three accidental entries shown in the screenshot (`No More Bets`, `But Always`, `Ikkyu-san`) using the existing DELETE movie API, plus retained TMDb warmup for first-search latency. Cache busts: `app.js?v=231`, `films-components.js?v=35`. **Validation**: `node --check` for app, films components, movies route, movie repository, TMDb client, and related tests; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js test/tmdbClient.test.js test/lightChromeCss.test.js` (107 passing). |
| 2026-05-10 11:24 Asia/Shanghai | Codex | Made Films TMDb search live and responsive without changing routing/sidebar/topbar/detail UI. The Films search input now schedules debounced TMDb search on typing, keeps Enter/Search button as immediate triggers, supports CJK 1-char and Latin 2-char minimums, keeps previous results visible while loading, aborts/ignores stale requests, uses a small in-memory result cache, and restores input focus/cursor after search re-renders. Search result save actions remain mutation-only on the Films index. Cache busts: `app.js?v=225`, `films-components.js?v=29`, `media-library.css?v=210`. **Validation**: `node --check` for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (93 passing). |
| 2026-05-10 12:29 Asia/Shanghai | Codex | Refined Films TMDb search transitions without changing routing/sidebar/topbar/detail/data. Search now keeps old results visible while loading, enforces a small 180ms minimum loading window to avoid flicker, tracks `filmSearchResultKey`/`filmSearchSettling` for result-set transitions, adds a subtle inline searching status, gives the result grid a stable min-height, and fades/slides new result cards in with reduced-motion support. Cache busts: `app.js?v=227`, `films-components.js?v=31`, `media-library.css?v=211`. **Validation**: `node --check` for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (93 passing). |
| 2026-05-10 12:43 Asia/Shanghai | Codex | Continued Films interaction polish without changing layout or data model. Clearing the TMDb search now fades existing results out over 180ms instead of dropping the panel instantly, and search-result `想看` / `看过` saves now run as silent optimistic mutations so success does not show a toast or re-trigger visible result-panel noise; failures still surface as errors. Cache busts: `app.js?v=228`, `films-components.js?v=32`, `media-library.css?v=212`. **Validation**: `node --check` for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (93 passing). |
| 2026-05-10 13:08 Asia/Shanghai | Codex | Tightened Films TMDb search latency after live screenshot feedback. Debounce dropped from 350ms to 180ms, the minimum loading guard dropped from 180ms to 80ms, and Chinese IME `compositionend` now clears any pending debounce and searches immediately so committed CJK characters do not wait another cycle. Cache bust: `app.js?v=229`, `films-components.js?v=33`. **Validation**: `node --check` for `app.js` and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (93 passing). |
| 2026-05-10 13:11 Asia/Shanghai | Codex | Narrowly brightened the Films Detail right-side backdrop after live screenshot feedback that the still was too faint, especially on its left side. Kept the layout and detail composition unchanged, increased backdrop image opacity/brightness/saturation, and reduced only the mid-left/right overlay darkness so the still reads more clearly while preserving text protection. Cache bust: `media-library.css?v=213`. **Validation**: `node --check` for `app.js` and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js test/lightChromeCss.test.js` (101 passing). |
| 2026-05-10 11:58 Asia/Shanghai | Codex | Hotfixed Films TMDb live search for Chinese IME input. Search composition now tracks `filmSearchComposing`, clears pending debounce on `compositionstart`, avoids render/search while pinyin is still composing, and schedules the live TMDb search only after `compositionend`; Enter is ignored during IME composition so pinyin no longer gets submitted as full-width/unfinished text. Cache bust: `app.js?v=226`, `films-components.js?v=30`. **Validation**: `node --check` for `app.js` and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (93 passing). |
| 2026-05-10 11:13 Asia/Shanghai | Codex | Fixed Film Detail rating UX regression. The active detail patch now guards stale disconnected DOM before `replaceWith`, preventing blur/change save races from surfacing `node to be removed is no longer a child` errors. Detail rating stars now use a visible gold foreground fill layer instead of a weak background effect, and the rating slider track is drawn by stable overlay rails using the real 0.5-5.0 slider progress so the gold fill no longer protrudes past the thumb. Cache busts: `app.js?v=224`, `films-components.js?v=28`, `media-library.css?v=209`. **Validation**: `node --check` for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (92 passing). |
| 2026-05-10 10:46 Asia/Shanghai | Codex | Refined Film Detail save UX after user rejected visible success toasts and save lag. Detail-page rating/date/notes/favourite/rewatch/status now use silent optimistic local updates: UI changes immediately, controls do not enter visible disabled saving state, success does not show green toast, and only failures toast plus roll back. User ratings now accept 0.1 increments from 0.5 to 5.0 while keeping five-star visual display, with backend validation updated from 0.5 steps to 0.1 steps. Cache busts: `app.js?v=223`, `films-components.js?v=27`, `media-library.css?v=208`. **Validation**: `node --check` for `app.js`, `films-components.js`, `movieRepository.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (92 passing). |
| 2026-05-10 10:16 Asia/Shanghai | Codex | Refined Film Detail inline editing toward direct manipulation. `MY NOTES` edit mode no longer renders Save/Cancel/Preview controls; it opens a focused Markdown textarea and autosaves raw Markdown on outside click, route change, Back to Films, or another film/detail action while still allowing the original click to continue. Watched date in Film Detail also drops the explicit Save button and persists on change/blur without duplicate blur saves. Cache busts: `app.js?v=222`, `films-components.js?v=26`, `media-library.css?v=207`. **Validation**: `node --check` for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (92 passing). |
| 2026-05-09 18:55 Asia/Shanghai | Codex | Continued Films interaction completion. The detail modal now lets users edit and persist `UserMovieEntry.watchedAt` separately from TMDb metadata; marking watched still defaults watched date to today, while later edits preserve local rating/director/cache data. Local Films filter chips are now real actions wired to app state (`All / Watched / Watching / Watchlist / Favorites`) instead of disabled decorative controls. Cache busts: `app.js?v=214`, `films-components.js?v=19`, `media-library.css?v=194`. **Validation**: syntax checks for `app.js` and `films-components.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (89 passing). |
| 2026-05-09 19:18 Asia/Shanghai | Codex | Restyled the Films detail rating control after user feedback that the previous slider/text were illegible. The same local-only 5-star half-step `UserMovieEntry.userRating` data path now renders as a high-contrast dark rating panel with large star preview, large numeric value, visible blue range track/thumb, live fill updates, and clearer rating mood text. Cache busts: `app.js?v=215`, `films-components.js?v=20`, `media-library.css?v=195`. **Validation**: syntax checks for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (89 passing). |
| 2026-05-09 19:47 Asia/Shanghai | Codex | Converted Films detail from a modal-style overlay into a full inner detail page rendered inside the Films main content area while preserving the sidebar and topbar chrome. Added `FilmDetailPage()` with a cinematic dark panel, large poster/backdrop, back-to-Films control, serif title/original title, metadata chips, integrated user/TMDb rating, synopsis, notes, and quiet bottom actions. `app.js` now renders this page in the Films route when `filmDetailOpen` is active and no longer mounts the Films detail overlay at the app-shell bottom. Cache busts: `app.js?v=216`, `films-components.js?v=21`, `media-library.css?v=196`. **Validation**: syntax checks for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (89 passing). |
| 2026-05-09 18:22 Asia/Shanghai | Codex | Fixed the Films footer spotlight semantics after user clarified the ring behavior. The footer now always keeps the right-side ring; unrated entries show `待`, while rated entries show only the user's local `UserMovieEntry.userRating`. TMDb scores are not used as the card ring. Also fixed rating saves wiping director metadata by merging partial movie payloads with existing MovieCache and preserving cached `director`. Cache busts: `app.js?v=213`, `films-components.js?v=18`, `media-library.css?v=193`. **Validation**: syntax checks for `films-components.js` and `movieRepository.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/tmdbClient.test.js` (92 passing). |
| 2026-05-09 18:11 Asia/Shanghai | Codex | Tightened the saved Films footer spotlight after user feedback that the previous director/ring area was oversized and that scores must be user-set. The footer director block and ring are now much smaller (`58px` ring, reduced director typography), and the ring renders only when `UserMovieEntry.userRating` exists; TMDb `voteAverage` is no longer used as a card ring fallback. Cache busts: `app.js?v=212`, `films-components.js?v=17`, `media-library.css?v=192`. **Validation**: syntax checks for `films-components.js` and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/tmdbClient.test.js` (91 passing). |
| 2026-05-09 18:00 Asia/Shanghai | Codex | Corrected the saved Films card footer placement per user clarification: the content below the dashed perforation is now the reference-style spotlight area (`DIRECTOR` + director name on the left, circular score ring on the right) instead of `TMDB #... / WATCHED`. The title/body area no longer carries the rating ring, and footer score uses user rating when present, otherwise TMDb `voteAverage` as an explicitly external display only. Cache busts: `app.js?v=211`, `films-components.js?v=16`, `media-library.css?v=191`. **Validation**: syntax checks for `films-components.js`, `app.js`, `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/tmdbClient.test.js` (91 passing). |
| 2026-05-09 17:46 Asia/Shanghai | Codex | Fixed why existing saved Films cards still did not show directors after the director data-flow commit: list hydration only read old MovieCache records and did not refresh missing `director` fields. `MovieRepository.listUserEntries()` now best-effort backfills missing directors by loading fresh TMDb detail during hydration, while keeping the local list available if that backfill fails. Added a regression covering old cached movies gaining `James Cameron` from detail refresh. **Validation**: syntax checks for `movieRepository.js` and `movieRepository.test.js`; targeted `mocha test/movieRepository.test.js test/moviesRoute.test.js test/tmdbClient.test.js test/previewActions.test.js` (93 passing). |
| 2026-05-09 17:34 Asia/Shanghai | Codex | Corrected the saved Films card direction after the rating-reference screenshot: the card now prioritizes a visible `DIRECTOR` info block when TMDb supplies director data, and the circular rating ring remains user-rating-only instead of showing fake `NR` or TMDb `voteAverage`. TMDb detail requests now include `append_to_response=credits`, `normalizeTmdbMovie()` extracts director names, `MovieCache` persists `director`, stale/missing-director detail caches refresh, and `normalizeMovieRecord()` passes director into `FilmCard()`. Cache busts: `app.js?v=210`, `films-components.js?v=15`, `media-library.css?v=190`. **Validation**: targeted `mocha test/tmdbClient.test.js test/movieRepository.test.js test/moviesRoute.test.js test/previewActions.test.js test/lightChromeCss.test.js` (100 passing). |
| 2026-05-09 17:23 Asia/Shanghai | Codex | Restyled the saved Films user rating badge to match the latest reference: the previous rectangular rating chip is now an 86px dark circular badge with a gold conic ring and large centered score; the rating label is visually hidden, with mobile sizing reduced to 72px. Cache bust bumped to `media-library.css?v=189`. **Validation**: `node --check js/media-library/films-components.js`; targeted `mocha test/lightChromeCss.test.js test/previewActions.test.js` (88 passing). |
| 2026-05-09 17:16 Asia/Shanghai | Codex | Removed the saved Films card barcode after user rejected the visual direction. `FilmCard()` now renders a quieter ticket metadata footer with TMDb/entry serial and watch status instead of barcode markup; CSS removes barcode styling and uses a compact two-field meta row under the perforation. Cache busts: `films-components.js?v=14`, `media-library.css?v=188`. **Validation**: syntax checks for `films-components.js`, `app.js`, `previewActions.test.js`; targeted `mocha test/lightChromeCss.test.js test/previewActions.test.js` (88 passing). |
| 2026-05-09 17:04 Asia/Shanghai | Codex | Refined the saved Films barcode after user feedback that it lacked printed texture and was too tall. The barcode is now a shorter white printed strip (`28px` high) with black ink bars, subtle paper/print texture, centered width, and softer ink opacity instead of white glowing bars on black. Cache bust bumped to `media-library.css?v=187`. **Validation**: `node --check test/lightChromeCss.test.js`; targeted `mocha test/lightChromeCss.test.js test/previewActions.test.js` (88 passing). |
| 2026-05-09 16:55 Asia/Shanghai | Codex | Corrected the saved Films ticket bottom cutouts after user feedback that the previous scallop looked like sharp sawteeth. Enlarged and spaced the bottom holes into a smoother bridge-arch rhythm (`32px` repeat, `24px` height) while preserving the theme-aware `--cml-film-cutout-bg` surface token. Bumped `media-library.css?v=186`. **Validation**: `node --check test/lightChromeCss.test.js`; targeted `mocha test/lightChromeCss.test.js test/previewActions.test.js` (88 passing). |
| 2026-05-09 16:47 Asia/Shanghai | Codex | Refined saved Films card footer to match the provided barcode-ticket reference more closely. Removed the `Archive note` footer text from `FilmCard()`, expanded the saved-card barcode to 46 dense variable-width bars, made it full-width/high-contrast, changed the footer perforation to a stronger dashed white line, enlarged the bottom semicircle cutouts while keeping them tied to `--cml-film-cutout-bg`, and bumped `films-components.js?v=13` plus `media-library.css?v=185`. Added regression coverage that saved card footers are barcode-only and keep theme-aware cutouts. **Validation**: syntax checks for `films-components.js`, `app.js`, `previewActions.test.js`, `lightChromeCss.test.js`; targeted `mocha test/lightChromeCss.test.js test/previewActions.test.js` (88 passing). |
| 2026-05-09 16:31 Asia/Shanghai | Codex | Refined the saved Films ticket footer to match the reference-bottom direction without hardcoding black cutout colors. `.cml-film-card` now defines semantic `--cml-film-cutout-bg` from `--cml-theme-content-panel-bg`; both side notches and the bottom scallop holes use that token, so theme/content-panel changes keep the cutouts visually transparent. The bottom barcode is taller, denser, and high-contrast white like the reference, with extra footer breathing room on desktop and mobile. Cache bust bumped to `media-library.css?v=184`. Added a CSS contract test locking the theme-aware cutout path. **Validation**: `node --check test/lightChromeCss.test.js`; `node --check js/media-library/films-components.js`; targeted `mocha test/lightChromeCss.test.js test/previewActions.test.js` (87 passing). |
| 2026-05-08 21:05 Asia/Shanghai | Claude | Production-stability bug sweep landed three concrete fixes and restored the full automated suite to green. `functions/upload/exifExtractor.js` no longer leaves embedded HEIC/HEIF preview extraction in a stubbed-out half-state: the capability gate, best-effort extractor path, test hooks, and preview MIME detection are wired again so embedded preview extraction behaves consistently for upload and `/file?...preview=1` flows. `test/functionsImportExtensions.test.js` now resolves the repo `functions/` tree from `import.meta.url` and skips `node_modules` / `.wrangler`, so it validates our source imports instead of depending on the caller CWD or third-party packages. `test/fetchResRoute.test.js` was aligned with the current fail-closed auth contract by sending a configured `authCode`, so the suite exercises the SSRF/redirect protections instead of stopping at 401. **Validation**: targeted Mocha for `exifPreviewExtractor`, `heicPreviewRoute`, `functionsImportExtensions`, and `fetchResRoute` all green; full suite now reports `294 passing`. |
| 2026-05-08 19:25 Asia/Shanghai | Claude | Fully redesigned the Films grid card from the rejected poster+stub mockup into a more editorial movie-diary card. `js/media-library/films-components.js` now renders each card as a unified cover + archive-ticket layout: stronger poster title/kicker/chip on the upper half, Chinese title as the primary ticket heading, English title as subtitle, grouped `Director / Release / Locale / Watched` info blocks, optional rating badge only when rating exists, softer perforation divider, and a much weaker barcode/footer detail. `css/media-library.css` replaced the old notch-based split card styling with a single dark shell, fuller cover overlays, calmer serif/sans hierarchy, fixed lower-panel rhythm, and tighter mobile rules so cards stay visually consistent across the grid. Cache/version sync: `films-components.js?v=7`, `app.js?v=201`, `media-library.css?v=177`. **Validation**: `node --check js/media-library/{films-components,components,app}.js`; `mocha test/previewActions.test.js` (73 passing); local Edge previews verified on `/dashboard#/films` for desktop and mobile. |
| 2026-05-08 16:55 Asia/Shanghai | Claude | Closed the Cloudflare Pages incident. Root cause was a bad mix of deploy-surface problems: legacy checked-in `*.gz` assets caused document/static object anomalies, while Functions packaging separately hit missing nested deps and then `exifr` / Sentry compatibility issues under Cloudflare’s Wrangler 3.101.0 path. Final recovery path was: remove all tracked `*.gz` static artifacts, ignore future `*.gz`, force `index.html` / `css/media-library.css` reupload, add a static `health.html` probe, define a root `install` script so Cloudflare installs `/functions` deps, remove runtime `exifr` from Functions and drop `exifr` from `functions/package.json`, then temporarily isolate Sentry/tracing middleware. Verified end state: `/`, `/health`, and `css/media-library.css?v=176` all return `200`. |
| 2026-05-08 16:40 Asia/Shanghai | Claude | Static asset purge pass per user instruction: removed every tracked `*.gz` artifact from the repo (all checked-in precompressed files under `css/` and `js/`), added `*.gz` to `.gitignore`, changed `index.html` and `css/media-library.css` with harmless reupload comments, bumped the media-library CSS query to `v=176`, and reduced root `health.html` to plain `OK` so Cloudflare Pages must publish fresh static objects rather than reusing legacy compressed artifacts. |
| 2026-05-08 16:25 Asia/Shanghai | Claude | Static-layer isolation pass for homepage 500s: confirmed root files `index.html` (1218 bytes) and `css/media-library.css` (316180 bytes) both exist, are non-empty, and are not symlinks; confirmed root `_headers`, `_redirects`, and `_routes.json` do not exist; added a harmless HTML comment to `index.html`, a harmless CSS comment to `css/media-library.css`, and a tiny static `health.html` probe so Pages must re-upload fresh document/static assets instead of reusing any stale edge artifact. |
| 2026-05-08 16:05 Asia/Shanghai | Claude | Ran the agreed runtime isolation experiment for homepage 500s: removed top-level `@cloudflare/pages-plugin-sentry` / `@sentry/tracing` imports from `functions/utils/middleware.js`, downgraded `errorHandling` / `telemetryData` to plain pass-through middleware, preserved `checkDatabaseConfig`, and removed both Sentry packages from `functions/package.json`. Functions-scope grep confirmed no remaining runtime imports, and local Pages Functions build still compiled successfully. |
| 2026-05-08 15:35 Asia/Shanghai | Claude | Follow-up to the exifr runtime removal: deleted `exifr` from `functions/package.json`, reinstalled the `/functions` dependency tree, and confirmed `npm --prefix ./functions ls exifr` now reports `(empty)`. This prevents Cloudflare Wrangler 3.101.0 from even seeing `functions/node_modules/exifr/index.d.ts` during Pages Functions packaging. |
| 2026-05-08 15:20 Asia/Shanghai | Claude | Removed the Pages Functions runtime dependency on `exifr` completely after the user confirmed Cloudflare 4935398 still failed on `functions/node_modules/exifr/index.d.ts` under Wrangler 3.101.0. Replaced `functions/upload/exifExtractor.js` with a local lightweight JPEG APP1 Exif parser that only extracts capture time (`DateTimeOriginal`, fallback `DateTimeDigitized` / `DateTime`) and keeps non-JPEG / malformed input on graceful null fallback. Updated EXIF-related tests to use synthetic JPEG Exif buffers instead of mocking `exifr.parse`. |
| 2026-05-08 14:55 Asia/Shanghai | Claude | Cloudflare Pages Functions packaging advanced past missing dependencies and exposed the next real runtime-bundling issue: `functions/upload/exifExtractor.js` imported bare `exifr`, and Wrangler/esbuild on Pages resolved that package to `functions/node_modules/exifr/index.d.ts` instead of the runtime module. Switched the import to `exifr/dist/full.esm.mjs`, keeping behavior unchanged while forcing the bundler onto the real ESM implementation. |
| 2026-05-08 14:35 Asia/Shanghai | Claude | Corrected the previous Cloudflare deploy fix after new logs showed Pages ignores `postinstall` and explicitly executes `npm run install` when no build command is set. Removed the unsupported `wrangler.toml [build]` attempt and switched root `package.json` to define a real `install` script that runs `npm --prefix ./functions install --no-package-lock --progress=false`, matching the platform's actual behavior. |
| 2026-05-08 14:20 Asia/Shanghai | Claude | Cloudflare Pages still ignored the root `postinstall` trick and continued bundling `/functions` before the nested dependency tree was present. Added a checked-in `wrangler.toml` with an explicit Pages build command (`npm --prefix ./functions install --no-package-lock --progress=false`) plus `pages_build_output_dir = "."`, so the platform has an authoritative repo-side instruction to install Functions dependencies before generating Pages Functions. |
| 2026-05-08 14:05 Asia/Shanghai | Claude | Cloudflare deploy was still failing after `functions/package.json` because the platform only ran root `npm clean-install`, then bundled `/functions` without installing that nested package's deps. Added root `postinstall` to run `npm --prefix ./functions install --no-package-lock --progress=false`, so the Pages install phase now hydrates the Functions dependency tree before the Functions bundler starts. |
| 2026-05-08 13:35 Asia/Shanghai | Claude | Identified the next Cloudflare blocker after build-command cleanup: Pages now reaches the real `/functions` packaging step, but the Functions bundle cannot resolve npm dependencies like `@aws-sdk/client-s3`, `exifr`, `@cloudflare/pages-plugin-sentry`, and `@sentry/tracing`. Added `functions/package.json` with the Functions-only dependency set so Pages can install/bundle the server-side modules from the correct scope instead of failing the deploy before runtime. |
| 2026-05-08 13:15 Asia/Shanghai | Claude | Emergency Pages entry recovery follow-up: production static assets (`/js/...`, `manifest.json`, images) were returning `200`, while only document entry requests (`/`, `/index.html`, `/dashboard#/films`) stayed `500`. Because Pages Functions now compile successfully from the real `functions/` directory and the failure isolated to the HTML entry path, removed tracked `index.html.gz` so Cloudflare serves `index.html` directly instead of a repo-managed precompressed homepage artifact. |
| 2026-05-05 21:00 Asia/Shanghai | Claude | Removed the Films poster status badge per product preference: the grid-card poster area should stay visually clean with no `想看 / 已看 / 在看` chip on top. `js/media-library/films-components.js` no longer renders `.cml-film-card__badge` in `FilmCard()`, and the unused badge CSS block was removed from `css/media-library.css`. Kept the prior unrated fix (`rating > 0` only) intact, so watchlist cards still avoid `0.0 不推荐`. Version sync: `films-components.js?v=6`, `app.js?v=200`, `media-library.css?v=175`, and `index.html.gz` regenerated. **Validation**: `node --check js/media-library/app.js`, `node --check js/media-library/films-components.js`, `mocha test/previewActions.test.js` (73 passing), plus local headless screenshot of `/#/films` confirmed the poster area is clean with no status label. |
| 2026-05-05 20:12 Asia/Shanghai | Claude | Hotfixed the Films card badge/readability regression reported from the live watchlist screenshot. `css/media-library.css` now hard-locks the top-left dark badge to white text on a darker translucent background (`color: #fffaf0 !important`, `-webkit-text-fill-color: #fffaf0`, stronger border/shadow), so dark-chip + dark-text cannot reappear. `js/media-library/films-components.js` now treats grid-card ratings as present only when numeric rating `> 0`, which removes the erroneous `0.0 不推荐` block from watchlist/unrated cards while keeping rated cards intact. Version sync: `films-components.js?v=5`, `app.js?v=199`, `media-library.css?v=175`, and `index.html.gz` regenerated. **Validation**: `node --check js/media-library/app.js`, `node --check js/media-library/films-components.js`, `mocha test/previewActions.test.js` (73 passing), plus local headless watchlist screenshot confirmed readable white badge text and no unrated rating block. |
| 2026-05-05 19:57 Asia/Shanghai | Claude | Contrast-only Films pass on top of `957e95e`. Kept the compact ticket-card structure untouched and only tuned readability in `css/media-library.css`: stronger full-poster overlay, higher-contrast badge, stronger ticket title, clearer strong/body/meta text hierarchy, softer-but-visible dashed separators, and darker barcode bars. No markup, route, modal, mock-data, or backend changes. Version sync: `media-library.css?v=175`; `app.js` stayed `v=198`; `films-components.js` stayed `v=4`; `index.html.gz` regenerated. **Validation**: `node --check js/media-library/app.js`, `films-components.js`, `films-data.js`, `components.js`; `mocha test/previewActions.test.js` (73 passing); `mocha test/adminPanelDom.test.js` (7 passing); local headless Edge screenshot of `/#/films` confirmed the improved contrast/hierarchy. |
| 2026-05-05 19:13 Asia/Shanghai | Claude | Rewrote the Films grid cards into compact ticket-stub records and removed the rejected bulky poster+black-block layout. `js/media-library/films-components.js` now uses the clean `poster-panel + ticket-panel` structure with seam notches, dashed separator, compact metadata blocks, and barcode footer; grid cards no longer show journal text, note fallback, or any unrated `0.0 / 不推荐` placeholder, and watched date only renders when present. `css/media-library.css` replaced the old rejected card selectors with the new ticket-stub styling and fixed the malformed orphan fragment before `.cml-film-modal`. Version sync: `films-components.js?v=4`, `app.js?v=198`, `media-library.css?v=174`, and `index.html.gz` regenerated. **Validation**: `node --check js/media-library/films-components.js`, `node --check js/media-library/app.js`, plus local headless Edge screenshot of `/#/films` confirmed the compact ticket layout in the real UI. |
| 2026-05-03 10:04 Asia/Shanghai | Claude | 记录当前入口隔离与 legacy locale guard 的最终状态：1) `/dashboard` 已隔离 legacy bundle，不加载 `chunk-vendors.8dadfdfd.js` 和 `app.f0825045.js?v=2`；2) `/dashboard` 只运行 `ui-overrides.js?v=10` 与 `media-library/app.js?v=185`，media-library 正常显示；3) `/login` 仍加载 legacy bundle，但通过 `index.html` 中的临时 `JSON.parse` guard 兜住损坏的内置 locale payload；4) `/login` 已验证不白屏，不再显示 `login.adminTitle` / `login.username` 等 i18n key，会显示英文 fallback 文案；5) Console 中 `[legacy-locale]` warn 为预期行为，uncaught `JSON.parse` 46638 红错已消失；6) `index.html.gz` 已同步；7) 后续原则：不直接手改 `app.f0825045.js`，如需根治 legacy locale，应恢复同批次源码/构建链。 |
| 2026-05-03 00:40 Asia/Shanghai | Claude | 记录当前阶段稳定性状态：1) `ensureDocsFolders is not defined` 已修复并 push：commit `dc04990 fix: restore documents folder helper`，`app.js?v=185`；2) `Album detail -> Add photos -> select photos -> Add` 已验证不再弹 `Add to album` modal，album-first 流程已恢复；3) 当前仍存在 known issue：legacy dashboard bundle `js/app.f0825045.js?v=2` 模块 46638 的 `zh-CN` locale JSON payload 编码损坏，导致启动阶段 `JSON.parse` 报错：`Expected ',' or '}' after property value in JSON at position 25893`；4) 该 known issue 当前不影响 media-library 主流程，但会污染 Console；5) 后续修复原则：不要直接手改 minified bundle，优先查找源 locale/i18n 文件重新构建，或将 legacy dashboard 默认切换为英文 locale，不再逐字段硬修乱码中文 payload。 |
| 2026-05-02 22:17 Asia/Shanghai | Claude | 修掉了老 dashboard 启动阶段新的 `JSON.parse` 崩溃。用户控制台截图显示错误栈落在 `js/app.f0825045.js`，不是 media-library 本体；根因是这个 legacy Vue bundle 内联的 `zh-CN` locale JSON 本身已被 mojibake 截断，`JSON.parse('...')` 在模块初始化时直接炸掉。此次直接修编译产物：把 `js/app.f0825045.js` 中损坏的中文 locale payload 替换为可解析的 fallback payload，并同步重建 `js/app.f0825045.js.gz`；另外把 `index.html` 的 legacy app 入口改成 `/js/app.f0825045.js?v=2`，强制浏览器拉取新 bundle，避免继续命中旧缓存。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\app.f0825045.js` 通过。 |
| 2026-05-02 21:43 Asia/Shanghai | Claude | 修掉了导致照片 preview 打不开/关不掉的直接运行时崩溃。用户控制台截图里已经给出根因：`app.js?v=183` 的 `renderPreviewTransientLayers()` 调用了不存在的 `getToastMarkup()`，所以无论打开 preview 还是执行 close 动画 finalize，只要走到 transient rerender 就会抛 `ReferenceError`，后续 UI 状态全部中断。现在在 `js/media-library/app.js` 补回统一的 `getToastMarkup()` helper，复用现有 toast DOM 结构并在 preview transient render 路径里安全输出 toast markup。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`；`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js test\previewOverlay.test.js`（74 passing）。 |
| 2026-05-02 21:32 Asia/Shanghai | Claude | 修掉了 preview 与 Add to album 侧栏串状态后“预览关不掉 / 后续预览打不开”的回归。根因有两段：一是 `js/media-library/components.js` 的 preview album panel 右上角 close 仍然走 `close-album-dialog`，用户感知上像要关整个 preview 却只是在关侧栏；二是 `js/media-library/app.js -> closePreview()` 在 `albumDialogOrigin === 'preview'` 时没有清掉 preview 专用 `selectedIds`，导致退出后仍残留 selection mode，后续点击 tile 被当成选中而不是打开 preview。现在 album panel 的 close 按钮直接改走 `close-preview`，并且 `closePreview()` 在 preview album flow 下会同步 `clearSelection({ shouldRender: false })`。`test/previewActions.test.js` 新增回归约束这两个 contract。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\components.js`、`app.js`、`test\previewActions.test.js`；`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js test\previewOverlay.test.js`（74 passing）。 |
| 2026-05-02 19:32 Asia/Shanghai | Claude | 按用户给的参考图，把 standard `Add to album` 目的地从大封面 grid tile 改成紧凑 row chooser：左侧 56px 小方形封面/占位，右侧 title + item count，整体更像目的地选择列表而不是相册墙。`js/media-library/components.js` 将 assign 模式 class 从 `cml-album-dialog__chooser-card` 切到 `cml-album-dialog__chooser-row`；`css/media-library.css` 去掉大卡片 min-height / 宽封面 / 阴影，改成单列 rows、轻 hover、selected inline ring/check，移动端同样保持小方形 row。同步更新 `test/previewActions.test.js` contract，并 bump `components.js?v=84`、`app.js?v=183`、`media-library.css?v=157`。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\components.js`、`app.js`、`test\previewActions.test.js`；`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js test\previewOverlay.test.js`（73 passing）。 |
| 2026-05-02 18:46 Asia/Shanghai | Claude | 按用户截图反馈收紧 Add to album 视觉 chooser 的 typography 和输入对比度。`css/media-library.css` 将 sheet 标题从夸张的大号 clamp 收到更普通的 22-30px 区间，压低说明文案、context strip、sort row、tile title/meta、check badge、封面高度和 grid 最小列宽，避免“老年模式”感；同时给 `.cml-album-dialog__search-input` 强制高对比 `#111827`、`-webkit-text-fill-color`、caret 和 placeholder opacity，抵消全局输入样式导致的浅色输入文字。同步 bump `components.js?v=83`、`app.js?v=182`、`media-library.css?v=156`。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\components.js`、`app.js`、`test\previewActions.test.js`；`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js test\previewOverlay.test.js`（73 passing）。 |
| 2026-05-02 18:35 Asia/Shanghai | Claude | 按用户反馈“别再是 low 的名字列表”，先把 standard `Add to album` 从线性行列表升级成更接近 Google Photos 的视觉选择器。`js/media-library/components.js` 的 `AlbumDialog()` assign 模式现在有 selection context strip、视觉 destination 文案、响应式 album chooser grid、封面/占位图 tile、已加入状态 check affordance，以及搜索无结果时的创建入口；仍保留 `assign-album` / `toggle-album-create` / `data-focus-key` 等原 action hook。`css/media-library.css` 为 sheet 增加了 context、chooser card、selected ring、empty chooser、移动端紧凑 row 回退样式。同步 bump `components.js?v=82`、`app.js?v=181`、`media-library.css?v=155`，并更新 `test/previewActions.test.js` 的 Add to album contract。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\components.js`、`app.js`、`test\previewActions.test.js`；`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js test\previewOverlay.test.js`（73 passing）。 |
| 2026-05-02 18:06 Asia/Shanghai | Claude | 接上并收尾今天下午没推完的 standard `Add to album` 弹窗任务。确认本地 `main` 只剩 `63117c0 fix: restore preview during album modal polish` 领先远端，代码树干净；该提交补回 `handleInput()` 里 create-name 输入的局部 patch 分支，让清空错误/输入新建 album 名时优先 `patchAlbumDialogCreateMode()`，避免全量 transient rerender 把 preview overlay 一起拆掉。重新验证 `js/media-library/app.js`、`components.js`、`test/previewActions.test.js` 语法，以及 `test/previewActions.test.js`（70 passing）。随后补齐 `Function_History.md` 和长期项目 baseline，并提交记录、推送到 `origin/main`。 |
| 2026-04-25 23:02 Asia/Shanghai | Codex | 按用户“做不了就别做，做就做好”的要求，收掉了 media-library preview 那套假 shared-element 动画，改成极简稳定的 overlay motion。`js/media-library/app.js` 里移除了 `previewTransitionRect` / shared ghost 几何链，预览打开改为轻量 enter class，关闭改为 close class + transitionend/fallback timer，仍保留关闭后回到原 tile/section 的 continuity，但不再做 tile→dialog 的缩放戏法；并额外处理了“刚打开立刻关闭”不会被 enter 中状态吞掉。`css/media-library.css` 则把 preview root 的旧 `scale(0.97)` keyframe 改成更克制的 backdrop opacity + panel translateY/opacity transition。顺手收了两处 shell-level UI debt：`SearchSummary` 的 limited-results notice 改成更明确的 `Incomplete results` + `Back to library / Refine search` affordance，preview mobile bottom bar 的 `download-preview` 文案/图标不再误写成 `Share`。缓存版本更新到 `components.js?v=81`、`app.js?v=170`、`media-library.css?v=150`。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`、`components.js`；`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js test\lightChromeCss.test.js`（69 passing）。 |
| 2026-04-25 22:28 Asia/Shanghai | Codex | 修掉了照片 preview 关闭时先闪黑场再退场的错误时序。根因在 `js/media-library/app.js -> closePreview()`：旧逻辑会在 overlay 还挂着时先执行 `restorePreviewPosition(previewId)`，把底层列表提前滚动/重排，再延迟进入 close 动画，于是用户会先看到一拍黑场。现在改成先执行 `animatePreviewCloseToTile(finalizeClosePreview)`，在 finalize 后再用 `requestAnimationFrame()` 恢复 tile / section 位置，保持“关闭后回到原位”的空间连续性，但不再让底层滚动抢在 preview 退场前发生。`test/previewActions.test.js` 也同步更新到新 contract。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`、`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js`（62 passing）。 |
| 2026-04-25 22:18 Asia/Shanghai | Codex | 按用户反馈收掉了 preview metadata 编辑成功后的 toast 噪音。`js/media-library/app.js` 里，description / date & time / video category / hidden album / tags 的保存成功提示，以及同类 rename dialog 成功提示，现已改成 silent success：保存成功只做局部 patch / render，不再弹 `... saved` / `... updated` toast；失败分支仍保留原有错误提示。`test/previewActions.test.js` 也同步把 contract 更新为“保留 preview-patch path，但不再要求 success toast”。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`、`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js`（62 passing）。 |
| 2026-04-25 22:05 Asia/Shanghai | Codex | 收口了 Telegram metadata recovery 这条后端链。functions/api/manage/metadata/[[path]].js 的 PATCH 白名单现在正式接受音频 Artist/Album；functions/api/manage/migrate/recover-tg-file-ids.js 新增 matches[] 输入，允许对时间戳风格 orphan 文件显式传入 key | messageId | chatId | channelName | fileId 做恢复，并继续保证恢复后只持久化 TgFileId/TgMessageId/TgChatId/ChannelName 这类安全字段；functions/api/manage/migrate/recover-tg-thumbnails.js 也对齐了相同的敏感字段策略，不再把 TgBotToken/TgProxyUrl 写回 per-file metadata。js/media-library/app.js / components.js 的 Cloud admin 面板此前已接上 Orphan match lines 输入，这次补齐了后端与回归测试。**验证**：D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\metadataRoute.test.js test\recoverTgFileIdsRoute.test.js test\recoverTgThumbnailsRoute.test.js test\adminPanelMigration.test.js test\lightChromeCss.test.js test\previewActions.test.js（87 passing）。 |
| 2026-04-09 ~23:22 | Codex | 收掉了一批残留的 Functions 裸相对 import 运行时兼容坑。`functions/api/_middleware.js`、`functions/api/directoryTree.js`、`functions/api/manage/_middleware.js`、`functions/api/manage/auth-session.js`、`functions/dav/_middleware.js`、`functions/file/_middleware.js`、`functions/random/_middleware.js`、`functions/upload/_middleware.js`、`functions/upload/chunkMerge.js`、`functions/upload/chunkUpload.js` 现在全部改成显式 `.js` 扩展名，避免 Node ESM 直载或某些测试路径下的 `ERR_MODULE_NOT_FOUND`。新增静态回归 `test/functionsImportExtensions.test.js`，递归扫描整个 `functions/` 目录，禁止未来再引入没有扩展名的相对 import。**验证**：`npm test` 现为 88 passing；`npx wrangler pages functions build --outdir '.wrangler\\functions-build'` 到 `Compiled Worker successfully`；尾部继续带已知 Windows 假错误 `The system cannot find the path specified.`，可忽略。 |
| 2026-04-25 23:02 Asia/Shanghai | Codex | 按用户“做不了就别做，做就做好”的要求，收掉了 media-library preview 那套假 shared-element 动画，改成极简稳定的 overlay motion。`js/media-library/app.js` 里移除了 `previewTransitionRect` / shared ghost 几何链，预览打开改为轻量 enter class，关闭改为 close class + transitionend/fallback timer，仍保留关闭后回到原 tile/section 的 continuity，但不再做 tile→dialog 的缩放戏法；并额外处理了“刚打开立刻关闭”不会被 enter 中状态吞掉。`css/media-library.css` 则把 preview root 的旧 `scale(0.97)` keyframe 改成更克制的 backdrop opacity + panel translateY/opacity transition。顺手收了两处 shell-level UI debt：`SearchSummary` 的 limited-results notice 改成更明确的 `Incomplete results` + `Back to library / Refine search` affordance，preview mobile bottom bar 的 `download-preview` 文案/图标不再误写成 `Share`。缓存版本更新到 `components.js?v=81`、`app.js?v=170`、`media-library.css?v=150`。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`、`components.js`；`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js test\lightChromeCss.test.js`（69 passing）。 |
| 2026-04-25 22:28 Asia/Shanghai | Codex | 修掉了照片 preview 关闭时先闪黑场再退场的错误时序。根因在 `js/media-library/app.js -> closePreview()`：旧逻辑会在 overlay 还挂着时先执行 `restorePreviewPosition(previewId)`，把底层列表提前滚动/重排，再延迟进入 close 动画，于是用户会先看到一拍黑场。现在改成先执行 `animatePreviewCloseToTile(finalizeClosePreview)`，在 finalize 后再用 `requestAnimationFrame()` 恢复 tile / section 位置，保持“关闭后回到原位”的空间连续性，但不再让底层滚动抢在 preview 退场前发生。`test/previewActions.test.js` 也同步更新到新 contract。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`、`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js`（62 passing）。 |
| 2026-04-25 22:18 Asia/Shanghai | Codex | 按用户反馈收掉了 preview metadata 编辑成功后的 toast 噪音。`js/media-library/app.js` 里，description / date & time / video category / hidden album / tags 的保存成功提示，以及同类 rename dialog 成功提示，现已改成 silent success：保存成功只做局部 patch / render，不再弹 `... saved` / `... updated` toast；失败分支仍保留原有错误提示。`test/previewActions.test.js` 也同步把 contract 更新为“保留 preview-patch path，但不再要求 success toast”。**验证**：`D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`、`D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\previewActions.test.js`（62 passing）。 |
| 2026-04-25 22:05 Asia/Shanghai | Codex | 收口了 Telegram metadata recovery 这条后端链。functions/api/manage/metadata/[[path]].js 的 PATCH 白名单现在正式接受音频 Artist/Album；functions/api/manage/migrate/recover-tg-file-ids.js 新增 matches[] 输入，允许对时间戳风格 orphan 文件显式传入 key | messageId | chatId | channelName | fileId 做恢复，并继续保证恢复后只持久化 TgFileId/TgMessageId/TgChatId/ChannelName 这类安全字段；functions/api/manage/migrate/recover-tg-thumbnails.js 也对齐了相同的敏感字段策略，不再把 TgBotToken/TgProxyUrl 写回 per-file metadata。js/media-library/app.js / components.js 的 Cloud admin 面板此前已接上 Orphan match lines 输入，这次补齐了后端与回归测试。**验证**：D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js test\metadataRoute.test.js test\recoverTgFileIdsRoute.test.js test\recoverTgThumbnailsRoute.test.js test\adminPanelMigration.test.js test\lightChromeCss.test.js test\previewActions.test.js（87 passing）。 |
| 2026-04-09 ~23:22 | Codex | 收掉了一批残留的 Functions 裸相对 import 运行时兼容坑。`functions/api/_middleware.js`、`functions/api/directoryTree.js`、`functions/api/manage/_middleware.js`、`functions/api/manage/auth-session.js`、`functions/dav/_middleware.js`、`functions/file/_middleware.js`、`functions/random/_middleware.js`、`functions/upload/_middleware.js`、`functions/upload/chunkMerge.js`、`functions/upload/chunkUpload.js` 现在全部改成显式 `.js` 扩展名，避免 Node ESM 直载或某些测试路径下的 `ERR_MODULE_NOT_FOUND`。新增静态回归 `test/functionsImportExtensions.test.js`，递归扫描整个 `functions/` 目录，禁止未来再引入没有扩展名的相对 import。**验证**：`npm test` 现为 88 passing；`npx wrangler pages functions build --outdir '.wrangler\\functions-build'` 到 `Compiled Worker successfully`；尾部继续带已知 Windows 假错误 `The system cannot find the path specified.`，可忽略。 |
| 2026-05-02 00:13 Asia/Shanghai | Codex | Fixed the album-first picker confirm action without touching the ordinary photo-first Add to album flow. In `js/media-library/app.js`, the `confirm-add-to-current-album` action now calls `commitSelectionToCurrentTarget()` directly instead of falling through the old broken confirm path. That means when `state.albumSelectionTarget` is set, the selected photos are committed straight into that current album and the flow closes back to the album detail page, instead of reopening the generic album chooser. Updated the picker selection-toolbar copy in `js/media-library/components.js` so current-album picker mode now shows explicit commit wording (`Add selected to <album>`) while other picker targets keep the generic wording. Added a regression in `test/previewActions.test.js` that locks three things at once: `confirm-add-to-current-album` must dispatch to `commitSelectionToCurrentTarget()`, that helper must still commit through `commitSelectionToAlbum(targetAlbum)`, and the album-first picker toolbar must not expose the ordinary `open-add-to-album` action. **Validation**: `D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`, `--check js\media-library\components.js`, and `--check test\previewActions.test.js` passed. Targeted `D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js .\test\previewActions.test.js` passed (`69 passing`). |
| 2026-05-02 00:28 Asia/Shanghai | Codex | Fixed the real album-first `Add photos` lifecycle bug in `js/media-library/app.js`. Root cause: `restoreNavigationFromHash()` always called `resetAddToTargetModes()` before replaying the current hash, so ordinary in-place `history.replaceState('#/photos')` refreshes during the album-first picker silently cleared `state.albumSelectionTarget` even though the user had not cancelled or exited the picker. That left the later `open-add-to-album` handler seeing an empty target and falling back to `openAlbumDialog('assign')`. `resetAddToTargetModes()` now accepts `{ preserveAlbumSelectionTarget }`, and `restoreNavigationFromHash()` preserves the current album target when replaying a `photos` route while an album-first picker target already exists. This keeps the target album alive through selection changes and route-sync renders, but still clears it on real picker exit paths. The direct album-first commit branch remains intact: `open-add-to-album` still short-circuits to `commitSelectionToAlbum(state.albumSelectionTarget)` when that target exists, while ordinary Photos multi-select still falls back to `openAlbumDialog('assign')`. Added a targeted regression in `test/previewActions.test.js` to lock the preserved album-target behavior during photos-hash restoration. **Validation**: `D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`; `D:\DevTools\nvm\v24.11.1\node.exe --check test\previewActions.test.js`; `D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js .\test\previewActions.test.js` (`70 passing`). |
| 2026-05-02 00:46 Asia/Shanghai | Codex | Fixed the live `Uncaught SyntaxError: missing ) after argument list` coming from the compiled legacy Vue bundle `js/app.f0825045.js`. Root cause: that tracked bundle contained a broken literal in the axios response interceptor (`r.nk.error("认证失败，请重新登录�?)`) where a mojibake replacement character had eaten the closing quote, so the browser parser died before the old app shell finished loading. Applied the smallest safe repair directly in the tracked compiled asset by restoring the literal to `r.nk.error("认证失败，请重新登录")`, then regenerated the paired gzip asset `js/app.f0825045.js.gz` so deployed clients and compressed responses stayed aligned. Bumped `index.html` media-library entry cache bust from `app.js?v=174` to `app.js?v=175`. **Validation**: `D:\DevTools\nvm\v24.11.1\node.exe --check js\app.f0825045.js` passed. |
| 2026-05-02 01:08 Asia/Shanghai | Codex | Reworked the standard `Add to album` modal in `js/media-library/components.js` to match the supplied information architecture while staying on the current theme palette rather than forcing a pure dark modal. The non-preview modal now renders as a larger sheet with search, scope chips (`All / My albums / Shared with me`), a `Last modified` row, a first-row `New album` entry, and full album rows with cover thumbnails + item counts instead of the old chip list plus bottom-only text field. Kept behavior intact by reusing the existing album dialog state in `js/media-library/app.js`: `albumDrawerSearch`, `albumDrawerScope`, `albumDrawerCreateMode`, `assignSelectionToAlbum(...)`, and `submitAlbumDialog()`. `setAlbumDrawerScope(...)` and `setPreviewAlbumCreateMode(...)` now work for the standard modal too instead of being preview-only. Updated `css/media-library.css` so the standard modal inherits the site theme but uses the richer sheet layout and album-entry presentation, while the preview drawer remains untouched. Expanded `test/previewActions.test.js` with a standard modal contract test for the new sheet structure and kept the existing album-first/photo-first behavior tests green. **Validation**: `D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\components.js`; `D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`; `D:\DevTools\nvm\v24.11.1\node.exe --check test\previewActions.test.js`; `D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js .\test\previewActions.test.js` (`70 passing`). |
| 2026-05-02 01:24 Asia/Shanghai | Codex | Finished the standard `Add to album` modal data wiring in `js/media-library/app.js`. The redesigned modal UI was already rendering correctly, but the non-preview render path still passed plain album-name arrays (`getDialogAlbumNames(...)` / `getAvailableAlbumNames(...)`) into `AlbumDialog(...)`, while the new sheet expects richer entry objects (`name`, `itemCount`, `coverUrl`, `scope`, `selected`). That mismatch produced the live `No items` rows instead of real albums. Both standard modal render call sites now pass `getDialogAlbumEntries(...)`, so the non-preview sheet receives the same album-entry structure as the preview drawer and can render real covers + counts. Bumped `index.html` media-library cache bust from `app.js?v=176` to `app.js?v=177`. **Validation**: `D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\app.js`; `D:\DevTools\nvm\v24.11.1\node.exe --check js\media-library\components.js`; `D:\DevTools\nvm\v24.11.1\node.exe --check test\previewActions.test.js`; `D:\DevTools\nvm\v22.14.0\node.exe .\node_modules\mocha\bin\mocha.js .\test\previewActions.test.js` (`70 passing`). |

| Time (Asia/Shanghai) | Task | Notes |
| --- | --- | --- |
| 2026-04-09 15:25 | Codex | Fixed the live Telegram media `500` regression behind broken dashboard thumbnails/previews. The root cause was inconsistent proxy-origin handling: `/file/...`, Telegram upload moderation, and Telegram sync import moderation all manually prefixed `https://` onto `proxyUrl`, which produced invalid URLs such as `https://http://127.0.0.1:7890/...` whenever the channel used a full proxy endpoint. Extracted shared helpers `resolveTelegramOrigin()` and `buildTelegramFileUrl()` in `functions/utils/telegramAPI.js`, switched the affected read/upload/sync paths to that helper, and added regression coverage in `test/telegramApi.test.js`. Also tightened `functions/utils/telegramFileId.js` so imported `tg_*` keys no longer misread short Telegram `file_unique_id` suffixes as real downloadable `file_id`s. Reverified with `node --check` on all touched backend files, full `npm test` at 52 passing, and local `npx wrangler pages functions build --outdir '.wrangler\\functions-build'` reaching `Compiled Worker successfully`. |
| 2026-05-09 09:24 Asia/Shanghai | Codex | Hardened `/api/manage/list` against a quota-burning degraded-mode bug. When the KV chunked index is unavailable or corrupted, the route now fails closed with `503` / `Index unavailable` instead of silently scanning KV directly and returning potentially unfiltered data. Added a regression in `test/d1Metadata.test.js` that seeds a corrupted `manage@index@meta` payload and locks the fail-closed response. **Validation**: `node --check functions/api/manage/list.js`; `node --check test/d1Metadata.test.js`; targeted `mocha test/d1Metadata.test.js` (17 passing); full suite now reports `301 passing`. |
| 2026-05-09 10:20 Asia/Shanghai | Codex | Removed a higher-load index-merge failure mode from the KV path. `mergeOperationsToIndex()` now short-circuits into a local multi-pass helper instead of depending on the old “remaining operations will be processed in subsequent calls” HTTP recursion path to make `readIndex()` succeed. This keeps reads stable when pending index operations exceed one batch (for example 31+ operations) and avoids turning backlog into self-fetch churn. Added a regression in `test/d1Metadata.test.js` that seeds 31 pending add operations and verifies `readIndex()` still returns all files successfully. **Validation**: `node --check functions/utils/indexManager.js`; `node --check test/d1Metadata.test.js`; targeted `mocha test/d1Metadata.test.js` (18 passing); full suite now reports `302 passing`. |
| 2026-05-09 10:47 Asia/Shanghai | Codex | Removed the same backlog pattern from atomic-operation cleanup. `deleteAllOperations()` now short-circuits into a local multi-pass delete helper instead of relying on the old `action=delete-operations` follow-up path, so large pending-operation queues are cleared in-process without recursive self-fetch. Added a regression in `test/d1Metadata.test.js` that seeds 41 pending operations and verifies one delete call clears them all. **Validation**: `node --check functions/utils/indexManager.js`; `node --check test/d1Metadata.test.js`; targeted `mocha test/d1Metadata.test.js` (19 passing); full suite now reports `303 passing`. |
| 2026-05-09 11:35 Asia/Shanghai | Codex | Softened the mobile library cold-start failure mode seen in the console screenshot. `js/media-library/app.js` now reduces the indexed `/api/manage/list` page size from `400` to `200`, raises the per-page timeout from `8000ms` to `12000ms`, and tags indexed timeout fallback separately as `librarySyncMeta.source = 'timeout'`. After repeated indexed timeouts with no DOM-scraped fallback items, the shell now exits the indefinite loading state instead of showing `Loading your library` forever. Added a source-contract regression in `test/previewActions.test.js` to lock the timeout fallback marker and the `timedOutWithoutFallback` loading gate. **Validation**: `node --check js/media-library/app.js`; `node --check test/previewActions.test.js`; targeted `mocha test/previewActions.test.js` (74 passing). |
| 2026-05-09 12:10 Asia/Shanghai | Codex | Fixed a structural Discord proxy-origin bug across file serving, moderation, and capture-time recovery paths. Added `resolveDiscordFileUrl(...)` in `functions/utils/discordAPI.js` so Discord CDN URLs now normalize through either a bare proxy host or a full proxy origin without producing invalid `https://http://...` URLs. Replaced duplicated string-splice logic in `functions/file/[[path]].js`, `functions/upload/index.js`, and `functions/api/manage/migrate/recover-capture-times.js`. Added `test/discordProxyUrl.test.js` to lock the helper behavior. **Validation**: `node --check functions/utils/discordAPI.js`; `node --check functions/file/[[path]].js`; `node --check functions/upload/index.js`; `node --check functions/api/manage/migrate/recover-capture-times.js`; targeted Mocha for `discordProxyUrl`, `heicPreviewRoute`, `fileRouteMetadata`, and `recoverCaptureTimesRoute` all green; full suite now reports `308 passing`. |
| 2026-05-09 12:28 Asia/Shanghai | Codex | Tightened file-read semantics for missing R2 objects. In `functions/file/[[path]].js`, the `CloudflareR2` branch now returns `404 Error: Image Not Found` when metadata still exists but the R2 object itself is gone, instead of misclassifying the condition as a `500` fetch failure. Added a regression in `test/heicPreviewRoute.test.js` to lock that contract. **Validation**: `node --check functions/file/[[path]].js`; `node --check test/heicPreviewRoute.test.js`; targeted `mocha test/heicPreviewRoute.test.js test/fileRouteMetadata.test.js` (5 passing). |
| 2026-05-09 12:44 Asia/Shanghai | Codex | Tightened Discord file-read semantics to match the R2 missing-object behavior. When Discord metadata exists but the message attachments no longer yield a resolvable file URL, `/file/...` now returns `404 Error: Image Not Found` instead of surfacing the missing remote attachment as a server `500`. Added `test/discordFileRoute.test.js` to lock the metadata-present / attachment-missing path. **Validation**: `node --check functions/file/[[path]].js`; `node --check test/discordFileRoute.test.js`; targeted `mocha test/discordFileRoute.test.js test/fileRouteMetadata.test.js test/heicPreviewRoute.test.js` (6 passing). |
| 2026-05-09 13:02 Asia/Shanghai | Codex | Aligned S3 file-read missing-object behavior with R2 and Discord. `handleS3FileViaAPI()` now maps AWS SDK `NoSuchKey` / `NotFound` / HTTP 404 errors to `404 Error: Image Not Found` instead of treating stale metadata plus a missing bucket object as a server `500`. Added a narrow test-only S3 client factory and a regression in `test/fileRouteMetadata.test.js` so the path is verified without real S3 access. **Validation**: `node --check functions/file/[[path]].js`; `node --check test/fileRouteMetadata.test.js`; targeted `mocha test/fileRouteMetadata.test.js test/heicPreviewRoute.test.js test/discordFileRoute.test.js` (7 passing). |
| 2026-05-09 13:18 Asia/Shanghai | Codex | Hardened the External file route against stale or malformed metadata. `/file/...` now validates `ExternalLink` before redirecting and returns `404 Error: Image Not Found` for missing, malformed, or non-HTTP(S) links instead of letting `Response.redirect()` throw an uncontrolled route error. Added `test/externalFileRoute.test.js` to cover both malformed-link 404 and valid-link 302 behavior. **Validation**: `node --check functions/file/[[path]].js`; `node --check test/externalFileRoute.test.js`; targeted `mocha test/externalFileRoute.test.js test/fileRouteMetadata.test.js test/heicPreviewRoute.test.js test/discordFileRoute.test.js` (9 passing). |
| 2026-05-09 13:31 Asia/Shanghai | Codex | Hardened External upload metadata at the write boundary. `functions/upload/index.js` now normalizes external upload URLs before storing `ExternalLink`, accepts only HTTP(S), and rejects empty, malformed, `ftp:`, or `javascript:` values with `400 Error: Invalid external URL` instead of writing stale/unservable metadata. Added `test/externalUploadUrl.test.js` for the normalization contract. **Validation**: `node --check functions/upload/index.js`; `node --check test/externalUploadUrl.test.js`; targeted `mocha test/externalUploadUrl.test.js test/externalFileRoute.test.js test/fileRouteMetadata.test.js test/heicPreviewRoute.test.js test/discordFileRoute.test.js` (11 passing). |
| 2026-05-09 14:02 Asia/Shanghai | Codex | Cleaned up the console warnings shown in the mobile dashboard screenshot. `js/entry-loader.js` now keeps the legacy locale JSON.parse fallback silent for the already-known corrupted built-in locale payload, so the guard still prevents white-screening without filling Console with `[legacy-locale]` warnings. `PrivateAlbumGate()` now renders a hidden username field plus a named password field, clearing Chrome's password-form accessibility/autofill warning. Cache versions bumped to `entry-loader.js?v=4`, `app.js?v=202`, and internal `components.js?v=90`. **Validation**: `node --check js/entry-loader.js`; `node --check js/media-library/components.js`; `node --check js/media-library/app.js`; targeted `mocha test/entryLoader.test.js test/previewActions.test.js` (83 passing). |
| 2026-05-09 14:18 Asia/Shanghai | Codex | Restored embedded HEIC/HEIF preview extraction to a lazy-load path. `functions/upload/exifExtractor.js` now loads `exifr/dist/full.esm.mjs` only on demand, caches the runtime thumbnail extractor after the first successful load, and caches the missing-export case so repeated HEIC preview attempts do not keep retrying a broken runtime loader. Added test hooks for the module loader and expanded `test/exifPreviewExtractor.test.js` to lock lazy success and one-shot failure behavior. **Validation**: `node --check functions/upload/exifExtractor.js`; `node --check test/exifPreviewExtractor.test.js`; targeted `mocha test/exifPreviewExtractor.test.js test/heicPreviewRoute.test.js` (7 passing). |
| 2026-05-09 13:49 Asia/Shanghai | Codex | Implemented the Films / Filmo-like data-source MVP without broad UI or architecture rewrites. Added `TMDbClient` with token-based TMDb search/detail requests, DTO normalization, clear `TMDB_ACCESS_TOKEN` configuration errors, and no hardcoded credentials. Added `MovieRepository` on the existing KV/D1 adapter style with separated `MovieCache` and `UserMovieEntry` data, 7-day detail-cache refresh, local-first user entries, status filtering, and deletion. Added `/api/manage/movies` for search/detail/list/save/delete, then wired the existing Films page to a minimal TMDb search strip, result list, detail open, and `加入想看` / `标记看过` save flow. Cache versions bumped to `app.js?v=203`, `films-components.js?v=8`, `films-data.js?v=4`, and `media-library.css?v=178`. **Validation**: node syntax checks for changed backend/frontend/tests; targeted Mocha for TMDb/repository/route/preview contracts (82 passing); full Mocha suite (323 passing). |
| 2026-05-09 14:07 Asia/Shanghai | Codex | Hotfixed the Films MVP search feedback reported from live screenshots. The `/api/manage/movies` 503s were real backend responses, most likely because the deployed Cloudflare environment does not yet expose `TMDB_ACCESS_TOKEN`; the frontend made that look like “Enter did nothing” because the TMDb result/error panel was rendered after the local film timeline and below the fold. `FilmsPage()` now treats the search controls as a real `data-form="films-search"` submit form so Enter and the Search button share one path, renders the TMDb feedback panel directly under the Films hero and above filters/local cards, and maps missing-token errors to a clear Cloudflare env-var instruction. Cache bust bumped to `app.js?v=204`. Added a previewActions contract locking submit-form search and above-list feedback placement. **Validation**: `node --check` for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/moviesRoute.test.js` (77 passing). |
| 2026-05-09 14:16 Asia/Shanghai | Codex | Tightened the Films TMDb error path after the live UI still showed only a generic `/api/manage/movies?... returned 503`. Added a movie-specific frontend JSON fetch helper so `/api/manage/movies` error payloads surface verbatim instead of collapsing to URL/status text, updated the visible error copy to explain missing TMDb credentials, and expanded the backend client to accept either `TMDB_ACCESS_TOKEN` (v4 bearer token) or `TMDB_API_KEY` (v3 query key). Cache bust bumped to `app.js?v=205`. **Validation**: `node --check` for `tmdbClient.js`, `app.js`, `films-components.js`, and related tests; targeted `mocha test/tmdbClient.test.js test/moviesRoute.test.js test/previewActions.test.js` (81 passing). |
| 2026-05-09 14:32 Asia/Shanghai | Codex | Removed the default mock/sample Films cards from the live diary path and tightened the TMDb-first add flow. `js/media-library/app.js` no longer imports or seeds `mockFilmRecords`; `applyMovieEntries()` now displays only locally saved TMDb-backed entries. `FilmsPage()` renders a proper empty state explaining that saved movies come from TMDb search. `FilmSearchResults()` now uses poster-first result cards with title/meta/overview and direct `想看` / `看过` buttons, while card body clicks still open detail. CSS was upgraded from debug rows to responsive addable movie cards with poster fallback, hover, action pills, and a stronger empty state. Cache bust bumped to `app.js?v=206`, `films-components.js?v=9`, `media-library.css?v=179`. **Validation**: `node --check` for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/tmdbClient.test.js test/movieRepository.test.js test/moviesRoute.test.js` (85 passing). |
| 2026-05-09 14:48 Asia/Shanghai | Codex | Restyled the saved Films grid cards toward the requested black ticket-stub reference while keeping the TMDb-first data flow unchanged. `FilmCard()` now surfaces a clean Chinese status badge on the poster, uses the year/runtime as ticket eyebrow metadata, and adds side perforation notch markup. `css/media-library.css` moved the saved cards from the light archive lower panel to a dark gold-on-black ticket body with dashed tear lines, scalloped barcode footer, stronger poster crop, and compact two-column mobile rules. Cache bust bumped to `app.js?v=207`, `films-components.js?v=10`, `media-library.css?v=180`. **Validation**: node syntax checks for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted Mocha for Films/TMDb contracts stayed green at 85 passing. |
| 2026-05-09 15:06 Asia/Shanghai | Codex | Corrected the Films ticket-stub palette after user feedback that black-gold felt too loud. `css/media-library.css` now removes the gold accent from saved film cards and uses the reference-style black/charcoal/gray treatment across card borders, poster glow, status chips, ticket body, rating block, dashed perforation, barcode, and hover state. Cache bust bumped to `media-library.css?v=181`; JS/data flow unchanged. **Validation**: node syntax checks for `app.js`, `films-components.js`, and `previewActions.test.js`; targeted Films/TMDb Mocha stayed green at 85 passing. |
| 2026-05-09 15:39 Asia/Shanghai | Codex | Implemented the Films user rating MVP on top of the TMDb-first movie source. `UserMovieEntry.userRating` is now a local-only 5-star half-step value (`0.5` to `5.0`, nullable for unrated/cleared) and no longer falls back to or converts from TMDb `voteAverage`; invalid explicit ratings fail with a clear error. `MovieCache` keeps external `voteAverage` and now also caches `voteCount` for display. The Films detail ticket now shows `TMDb rating` and `My rating` as separate rows, adds a simple half-star range slider plus clear-rating action, and saved cards show only the user's own rating badge. Save/status/rating actions now use optimistic local updates and disabled Saving states so `想看` / `看过` clicks feel immediate while the backend persists. Cache bust bumped to `app.js?v=208`, `films-components.js?v=11`, `media-library.css?v=182`. **Validation**: syntax checks for touched backend/frontend/tests; targeted Films/TMDb Mocha 88 passing; full Mocha suite 329 passing. |
| 2026-05-09 15:57 Asia/Shanghai | Codex | Cleaned up the saved Films ticket cards per screenshot feedback. `FilmCard()` no longer overlays `Movie diary`, watch-status badges, titles, genres, or any other text on top of the poster image, and the ticket body no longer renders the redundant year/runtime eyebrow above the film title because the `Release` row already carries that data. Cache bust bumped to `app.js?v=209` and `films-components.js?v=12`. Added a regression that slices the poster panel HTML and asserts no overlay labels/status text return. **Validation**: syntax checks for `films-components.js`, `app.js`, and `previewActions.test.js`; targeted Films/TMDb Mocha 89 passing. |
| 2026-05-09 16:08 Asia/Shanghai | Codex | Adjusted saved Films ticket notches so the side cutouts visually read as transparent holes against the Films panel. `.cml-film-card__notch` now uses `var(--cml-theme-content-panel-bg, rgb(21, 23, 27))` instead of the page background token, matching the black content surface shown in the screenshot. Cache bust bumped to `media-library.css?v=183`; JS unchanged. **Validation**: syntax checks for `films-components.js`, `previewActions.test.js`, and `lightChromeCss.test.js`; targeted `lightChromeCss + previewActions` Mocha 86 passing. |
| 2026-05-09 12:10 Asia/Shanghai | Codex | Fixed a structural Discord proxy-origin bug across file serving, moderation, and capture-time recovery paths. Added `resolveDiscordFileUrl(...)` in `functions/utils/discordAPI.js` so Discord CDN URLs now normalize through either a bare proxy host or a full proxy origin without producing invalid `https://http://...` URLs. Replaced duplicated string-splice logic in `functions/file/[[path]].js`, `functions/upload/index.js`, and `functions/api/manage/migrate/recover-capture-times.js`. Added `test/discordProxyUrl.test.js` to lock the helper behavior. **Validation**: `node --check functions/utils/discordAPI.js`; `node --check functions/file/[[path]].js`; `node --check functions/upload/index.js`; `node --check functions/api/manage/migrate/recover-capture-times.js`; targeted Mocha for `discordProxyUrl`, `heicPreviewRoute`, `fileRouteMetadata`, and `recoverCaptureTimesRoute` all green. |
| 2026-05-09 22:05 Asia/Shanghai | Codex | Removed the Films detail double-frame feel after user feedback that clicking a movie should not render “a content box inside another content box”. In detail state, the outer `cml-main-content-shell` now becomes a pure host surface (single column, no extra panel bg/border/shadow), `cml-main-content` padding collapses to zero, and `cml-film-detail-page` now fills the Films content area directly (no extra border radius/frame). Mobile detail state follows the same single-surface rule. Cache bust: `media-library.css?v=198`. **Validation**: syntax checks for `lightChromeCss.test.js` and `films-components.js`; targeted `mocha test/lightChromeCss.test.js test/previewActions.test.js` (89 passing). |
| 2026-05-09 22:16 Asia/Shanghai | Codex | Fixed the Films TMDb search-result save flow so clicking `想看` / `看过` only saves status and no longer flashes into the Film Detail Page. `saveFilmStatus()` now defaults to no detail navigation, film action clicks are intercepted before card fallback handling, and saved film card detail navigation is modeled as `#/films/:filmId` with `#/films` restoring the index. Back to Films now updates the hash back to `#/films`; non-Films routes clear stale detail state. Cache bust: `app.js?v=218`. **Validation**: syntax checks for `app.js` and `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (89 passing). |
| 2026-05-09 22:22 Asia/Shanghai | Codex | Softened the Films detail backdrop edges after user reported a visible straight boundary on the right-side still. The backdrop layer now uses a container mask plus longer multi-stop left/right and bottom gradients, keeping the still visible while feathering hard rectangular edges into the page surface. Cache bust: `media-library.css?v=199`. **Validation**: syntax check for `lightChromeCss.test.js`; targeted `mocha test/lightChromeCss.test.js test/previewActions.test.js` (90 passing). |
| 2026-05-09 21:59 Asia/Shanghai | Codex | Refined the Films inner detail page toward the premium cinematic mockup without changing routing, data, sidebar/topbar behavior, or Films grid. `FilmDetailPage()` now renders a real right-side backdrop image layer with poster fallback, while CSS adds layered charcoal/navy gradients, warmer editorial typography, softer metadata/notes rhythm, integrated poster shadowing, subtler action buttons, and a small sidebar wordmark clipping fix. Cache busts: `app.js?v=217`, `films-components.js?v=22`, `media-library.css?v=197`. **Validation**: syntax checks for `films-components.js`, `app.js`, `previewActions.test.js`, and `lightChromeCss.test.js`; targeted `mocha test/lightChromeCss.test.js test/previewActions.test.js`; Films/TMDb targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (89 passing). |
| 2026-05-09 23:08 Asia/Shanghai | Codex | Stabilized Films data/navigation flows without touching sidebar/topbar or unrelated modules. Added canonical `normalizeFilmRecord(...)` and routed `normalizeMovieRecord(...)` through it so TMDb/manual records share one shape; enforced tmdbId-first upsert de-duplication in `upsertFilmRecord(...)`; preserved local user fields (`userRating`, `note`, `journal`/`noteMarkdown`, `watchedAt`, `status`, `favorite`) when refreshing detail data (`openTmdbFilmDetail(..., preserveLocal: true)`). Search-result save remains mutation-only: `saveFilmStatus()` no longer writes blank watched dates for non-watched states and retains index route unless explicitly asked to open detail. Detail action buttons now expose explicit `data-action` names (`film-toggle-favourite`, `film-edit-notes`, `film-mark-rewatch`, `film-more-actions`) for next-step wiring. Route model remains `#/films` index and `#/films/:id` detail with Back-to-Films hash recovery. Cache bust: `app.js?v=219`, `films-components.js?v=23`. **Manual QA checklist**: search TMDb → save 想看/看过 (stay on index) → click saved card (open detail) → Back to Films (`#/films`) → browser back/forward → resave existing TMDb film (no duplicate by tmdbId). **Validation**: `node --check js/media-library/app.js js/media-library/films-components.js test/previewActions.test.js`; `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (90 passing). |
| 2026-05-10 09:27 Asia/Shanghai | Codex | Continued Filmo component completion in SUNDOWNER Films. Film Detail bottom actions are now real local data actions instead of decorative buttons: favourites toggle persists `UserMovieEntry.isFavorite`, rewatch marks watched with today's watched date, and notes open an inline Markdown editor with write/preview/save/cancel. `UserMovieEntry` now preserves multiline `journal` / `noteMarkdown` while keeping TMDb `MovieCache` separate. The detail notes view renders a small safe Markdown subset and blocks unsafe link hrefs. Cache busts: `app.js?v=220`, `films-components.js?v=24`, `media-library.css?v=205`. **Validation**: syntax checks for `app.js`, `films-components.js`, `movieRepository.js`, `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (92 passing). |
| 2026-05-10 09:51 Asia/Shanghai | Codex | Further reduced Films detail save latency by making local mutations patch the active Film Detail DOM directly before background persistence. Rating slider, watched-date save, favourite toggle, rewatch, and notes save now try `patchActiveFilmDetailView()` first and only fall back to full `render()` when the Films detail surface is not currently mounted. This keeps the response immediate on the active detail page while preserving the same persisted `UserMovieEntry` / `MovieCache` contract. Cache busts: `app.js?v=221`, `films-components.js?v=25`, `media-library.css?v=206`. **Validation**: syntax checks for `app.js`, `films-components.js`, `previewActions.test.js`; targeted `mocha test/previewActions.test.js test/movieRepository.test.js test/moviesRoute.test.js` (92 passing). |
