name: Release Metadata Check

on:
  release:
    types: [created, published, edited]
  workflow_dispatch:

jobs:
  release-metadata:
    runs-on: ubuntu-latest
    steps:
      - name: Print release metadata
        run: |
          echo "SUNDOWNER release metadata event received."
          echo "No external release repository is synchronized by this workflow."
