# Phase 3 接续文档 — leosDrive-telegram-sync 优化待办

> **给明天/新窗口的你**：本文件自包含，不依赖之前对话。直接照着做即可。
> 项目路径：`D:\Codex\midTime\leosDrive-telegram-sync`
> Git 远程：`Aschenbath/SUNDOWNER`，分支 `main`
> 收尾默认：每组改完 `node --check` 验证，全部完成后 `git add` + `commit` + `push`。

---

## 0. 背景：已经做完了什么 (Phase 1 + 2，commit a4be2ad 已 push)

2026-05-31 用 workflow 全盘扫了 201 个源文件，发现 91 bug + 39 优化点。
**已修复并 push 的 10 项（不要重复做）：**

| 项 | 文件 | 做了什么 |
|---|---|---|
| Bug1 | indexManager.js mergeOperationsToIndex | 删了 early return 后的 136 行死代码，保留 `mergeOperationsInProcess` |
| Bug2 | indexManager.js deleteAllOperations | 删了 early return 后的 24 行死代码，保留 `deleteOperationsInProcess` |
| Bug3 | server/r2Storage.js put() | stream reader 加 try-finally + releaseLock() |
| Bug4 | server/r2Storage.js uploadPart() | 同上 |
| Bug5 | functions/api/login.js | 加 IP rate limit (5次/5min) |
| Bug6 | functions/api/manage/auth-session.js | 加 IP rate limit (10次/5min) |
| Bug7 | databaseAdapter.js getMigrationStatus | 加 60s TTL cache |
| Opt1 | indexManager.js applyBatchAddOperation | O(n²) Map 重建 → O(n) |
| Opt2 | databaseAdapter.js listIndexOperations | 串行 await → Promise.all |
| Opt3 | huggingfaceAPI.js multipart | 串行上传 → 3 并发批次 |

**主动回退的：** CORS wildcard → 白名单的改动**已撤销**。项目用 Basic Auth/Bearer Token 不是纯 cookie session，wildcard 够用，加 `ADMIN_ALLOWED_ORIGINS` 环境变量属于过度工程。**Phase 3 不要再碰 CORS。**

**复用资产：** `functions/utils/rateLimiter.js` 已存在，导出 `checkRateLimit(key, kv, options)`、`getClientIp(request)`、`createRateLimitResponse(resetAt)`。KV binding 名是 `env.img_url`（全项目统一）。

> ⚠️ **行号警告**：indexManager.js 因 Phase 1 删了 ~160 行，下面给的行号是 **2026-05-31 commit a4be2ad 之后的当前行号**。如果又改过，用 grep 重新定位（每项都给了 grep 锚点）。

---

## Phase 3 待办（按建议顺序，从易到难）

### ============ A. 性能优化（高确定性，先做）============

#### A1. telegramSync 串行删除 → 并行 【影响:中 / 工作量:30min】
- **文件**：`functions/utils/telegramSync.js`
- **当前行号**：499-504，函数 `cleanupStaleImportedFiles`
- **grep 锚点**：`grep -n "for (const staleFileId of staleFileIds)" functions/utils/telegramSync.js`
- **当前代码**：
  ```javascript
  for (const staleFileId of staleFileIds) {
      await db.delete(staleFileId)
      await removeFileFromIndex(context, staleFileId)
  }
  return staleFileIds
  ```
- **问题**：每个 stale 文件串行删除，N 个文件 = 2N 次串行 I/O 往返。
- **改成**：
  ```javascript
  await Promise.all(staleFileIds.map(async (staleFileId) => {
      await db.delete(staleFileId)
      await removeFileFromIndex(context, staleFileId)
  }))
  return staleFileIds
  ```
- **注意**：确认 `removeFileFromIndex` 没有共享可变状态竞争（它内部走 addFileToIndex 体系，KV 模式下是写独立 operation key，并行安全；D1 模式各行独立，也安全）。如果担心，可加并发上限分批（参考 huggingfaceAPI.js 的 CONCURRENCY=3 批次写法）。
- **预期**：删除提速 ~50%（受并发往返收敛）。

#### A2. indexManager 排序优化 【影响:中 / 工作量:1h】
- **文件**：`functions/utils/indexManager.js`
- **两处当前行号**：
  - **209**：`files.sort((a, b) => (b.metadata?.TimeStamp || 0) - (a.metadata?.TimeStamp || 0));` —— 在 `getIndex`/加载后全量排序
  - **1081**：`newIndex.files.sort((a, b) => b.metadata.TimeStamp - a.metadata.TimeStamp);` —— 在 rebuild 时全量排序
- **grep 锚点**：`grep -n "metadata?.TimeStamp || 0\|metadata.TimeStamp" functions/utils/indexManager.js`
- **问题**：加载完所有记录后再对整个数组排序。大索引下是 O(n log n) 的一次性大开销。
- **两个方向（二选一）**：
  - **方向1（推荐，D1 模式）**：D1 查询时直接 `ORDER BY` 排好序返回，加载端跳过 sort。需查 `d1Database.js` 的 `loadAllIndexedFilesFromDatabase` 是否已带 ORDER BY；如果有，209 行的 sort 是冗余的，可在 D1 路径下跳过。
  - **方向2（KV 模式）**：用已有的 `insertFileInOrder` helper 在收集阶段增量插入保持有序，避免末尾全量 sort。
- **注意**：line 1081 用的是 `b.metadata.TimeStamp`（无可选链），如果有文件缺 metadata 会抛错——**顺手加可选链** `b.metadata?.TimeStamp || 0`。这其实也是个潜在 bug。
- **验证**：改完跑一次 rebuild，确认文件顺序仍是时间倒序（最新在前）。

---

### ============ B. 输入校验加固（安全相关，中优先）============

#### B1. sanitizeObject 递归深度限制 【影响:中(防DoS) / 工作量:30min】
- **文件**：`functions/api/manage/batch/index/chunk.js`
- **当前行号**：133（函数定义），143/149（递归调用），315（调用点）
- **grep 锚点**：`grep -n "function sanitizeObject" functions/api/manage/batch/index/chunk.js`
- **当前代码**（约 133-152）：
  ```javascript
  function sanitizeObject(obj) {
      ...
      return obj.map(item => sanitizeObject(item));   // line 143
      ...
      sanitized[sanitizeString(key)] = sanitizeObject(obj[key]);  // line 149
  }
  ```
- **问题**：无递归深度限制。恶意/畸形请求构造深度嵌套对象可栈溢出 crash worker。
- **改成**：加 depth 参数，超过阈值（建议 50）抛错或截断：
  ```javascript
  function sanitizeObject(obj, depth = 0) {
      if (depth > 50) {
          throw new Error('Object nesting too deep (max 50 levels)');
      }
      ...
      return obj.map(item => sanitizeObject(item, depth + 1));
      ...
      sanitized[sanitizeString(key)] = sanitizeObject(obj[key], depth + 1);
  }
  ```
  并在 315 行调用点用 try-catch 包住，深度超限返回 400。

#### B2. chunk 存储加 TTL（防存储泄漏）【影响:中 / 工作量:15min】
- **文件**：`functions/api/manage/batch/index/chunk.js`
- **当前行号**：332（chunkKey 定义），343（put）
- **grep 锚点**：`grep -n "chunk_\${sessionId}\|await db.put(chunkKey" functions/api/manage/batch/index/chunk.js`
- **当前代码**（line 343）：
  ```javascript
  await db.put(chunkKey, JSON.stringify(chunkData));
  ```
- **问题**：rebuild 用的临时 chunk 没有 TTL，如果 rebuild 失败/中断，这些 chunk 永久滞留 KV，长期累积存储膨胀。
- **改成**：加 24h 过期：
  ```javascript
  await db.put(chunkKey, JSON.stringify(chunkData), { expirationTtl: 86400 });
  ```
- **注意**：确认 rebuild 正常完成时会主动删除这些 chunk；TTL 是兜底。如果正常流程已删，TTL 只在异常时生效，无副作用。

#### B3. 路径遍历加固 【影响:中(安全) / 工作量:1-1.5h】
- **文件1**：`functions/api/manage/delete/[[path]].js`，line 23 `params.path = decodeURIComponent(params.path);`
- **文件2**：`functions/api/manage/move/[[path]].js`（dist 参数）
- **文件3**：`functions/api/manage/list.js`（目录参数 sanitize，约 259-297）
- **问题**：decodeURIComponent 后的路径未充分校验 `..`、绝对路径等遍历序列。delete 可能删到非预期文件，list 可能列出非预期目录。
- **改法**：解码后加显式校验，拒绝含 `..` 或绝对路径标记的路径。建议写一个共享 helper（放 `functions/utils/`）：
  ```javascript
  export function isPathSafe(path) {
      if (!path) return true;
      const normalized = path.replace(/\\/g, '/');
      // 拒绝 .. 遍历、绝对路径、空字节
      if (normalized.includes('..')) return false;
      if (normalized.startsWith('/')) return false;
      if (/[\0]/.test(normalized)) return false;
      return true;
  }
  ```
  在 delete/move/list 的路径解码后调用，不安全则返回 400。
- **注意**：先确认现有上传路径都是相对路径格式（看 `uploadTools.js` 的 `sanitizeUploadFolder`，它可能已有部分逻辑可复用——**优先复用而非新写**）。

---

### ============ C. 错误处理统一（量大、低风险、可选）============

> 这组是 15+ 个 "try-catch 吞错误" 类问题，**不影响功能**，主要改善可调试性。低优先，时间不够可跳过或只挑关键的做。

#### C1. 给 silent catch 加日志（快速批量）【工作量:2-3h】
逐个加 `console.error/warn` 带上下文。具体位置：
- `telegramSync.js:192` `loadAlbumCommandState` — JSON parse 错误静默删 key，加日志
- `telegramSync.js:222` `loadMomentsCommandState` — 同上
- `indexManager.js:232` `releaseRebuildLock` — 空 catch 块，至少加 log
- `indexManager.js:1461` `getAllPendingOperations` — 跳过解析失败的 operation 时加 log
- `sysConfig.js:100` `filterChannelsByQuota` — quota 检查失败静默放行，加 log

#### C2. sqliteD1.js 错误信息带参数 【工作量:30min】
- **文件**：`server/sqliteD1.js`，line 58/74/88（first/all/run 方法）
- 重新抛错时在 message 里带上脱敏后的 SQL 参数，方便调试。注意脱敏（别打印密码类字段）。
- **注意**：这是 Docker 本地开发模式的适配层，不影响生产 Cloudflare D1。优先级最低。

#### C3. sysConfig fetch 配置区分"无配置"vs"出错" 【工作量:1h】
- **文件**：`functions/utils/sysConfig.js`，line 128 `fetchUploadConfig`、142 `fetchSecurityConfig`
- 当前 catch 后返回空配置，调用方无法区分"没配"还是"读取出错"。建议返回带错误标志的对象或抛特定错误类型。
- **注意**：`fetchSecurityConfig` 已有 `hasSecurityConfigLoadError` 机制（login.js 在用），参考它的模式统一。

---

## 验证 & 收尾清单（每次必做）

```powershell
# 1. 语法检查所有改过的文件
cd D:\Codex\midTime\leosDrive-telegram-sync
node --check functions/utils/telegramSync.js
node --check functions/utils/indexManager.js
node --check functions/api/manage/batch/index/chunk.js
# ... 改了哪个查哪个

# 2. 跑测试（注意：better-sqlite3 原生模块此前有已知构建问题，
#    如果 mocha 起不来是环境问题不是代码问题，node --check 通过即可）
npm test

# 3. git 收尾
git status --short
git add functions/ server/
git commit -m "..."   # 结尾带 Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
git push
```

> ⚠️ **CRLF 警告正常**：commit 时 git 会警告 LF→CRLF，是 Windows 行尾，无害。
> ⚠️ **树脏不要盲 pull**：如果 `git status` 有未知改动，先看清楚再说，别覆盖。

---

## 重要约束（来自工作协议，别违反）

1. **canonical repo 在 D:\Codex\midTime\leosDrive-telegram-sync 本身**（这次确认 .git 在此，直接改这里）。
2. 进度写本文件同目录的 `history.md`；跨项目可复用结论才写 `D:/Codex/CodexRules.md`。
3. 报告文件（`COMPREHENSIVE_AUDIT_2026-05-31.md` / `CRITICAL_FIXES_2026-05-31.md` / `PERFORMANCE_OPTIMIZATIONS_2026-05-31.md` / 本文件）**未纳入 git commit**，是工作参考，不污染仓库。要不要 commit 它们由你定。

---

## 完整 91 bug / 39 优化原始数据

如需查其余未列入 Phase 3 的低优先项，原始 JSON 在：
`C:\Users\ASCHEN~1\AppData\Local\Temp\claude\C--Users-aschenbath\f47962b6-c025-4ab8-944d-ff5d8f060110\tasks\wx48yubmz.output`
（temp 目录可能被清，如已没了就重跑一次 audit workflow）。
关键剩余 medium 项还包括：
- `databaseAdapter.js` putWithRollback/deleteWithRollback：rollback 失败时只 log 原错误，建议抛复合错误（line ~236/263）
- `d1Database.js:226` backfillFileTypeBucket：失败不设哨兵，每次冷启动重试
- `huggingfaceAPI.js:39/82` repoExists/createRepoIfNotExists：catch 全部错误返 false，网络错误被当成"repo 不存在"
- `apiTokens.js:134`：列出 token 时返回前 15 字符，缩小爆破搜索空间，建议只返回 hash/id
